import 'package:flutter/widgets.dart';

const double kMediumPadding = 24.0; //this Padding is bigger than default padding;
const double kBottomBarIconSize = 20.0; //size of icon on the bottom bar
const double kItemPadding = 10.0; // df padding between child widgets
const double kTopPadding = 8.0; // df top padding
const double kMinPadding = 5.0; // minimum df padding
const double kDefaultPadding = 16.0; // standard padding usin df items


// this df icon size
const double kDefaultIconSize = 18;

// size of border Radious 
const BorderRadius kDefaultBorderRadious = BorderRadius.all(Radius.circular(10));